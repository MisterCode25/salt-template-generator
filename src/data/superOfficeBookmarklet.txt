javascript:(()=>{const norm=s=>String(s||"").replace(/\s+/g," ").trim();const showToast=msg=>{let t=document.createElement("div");t.textContent=msg;t.style.cssText="position:fixed;z-index:2147483647;left:50%;top:24px;transform:translateX(-50%);background:#111;color:#fff;padding:12px 18px;border-radius:10px;font:14px Arial,sans-serif;box-shadow:0 6px 22px rgba(0,0,0,.25);opacity:0;transition:opacity .2s ease;";document.body.appendChild(t);requestAnimationFrame(()=>t.style.opacity="1");setTimeout(()=>{t.style.opacity="0";setTimeout(()=>t.remove(),250)},1800)};const text=document.body.innerText;const ticketId=text.match(/REQUEST\s+(\d+)/)?.[1]||text.match(/\b(\d{8})\s+-\s+Votre demande/)?.[1]||null;let externalTicketId=null;const labels=[...document.querySelectorAll(".HtmlInfoFields2_label,td,th,div,span")].filter(e=>norm(e.innerText)==="External ticket ID:"||norm(e.innerText)==="External ticket ID");for(const label of labels){let tr=label.closest("tr");if(tr){const cells=[...tr.children];let i=cells.indexOf(label.closest("td,th"));for(let j=i+1;j<cells.length;j++){const v=norm(cells[j].innerText);if(v&&v!=="External ticket ID:"){externalTicketId=v;break;}}}if(externalTicketId)break;let td=label.closest("td,th");let next=td?.nextElementSibling;while(next){const v=norm(next.innerText);if(v){externalTicketId=v;break;}next=next.nextElementSibling;}if(externalTicketId)break;}if(!externalTicketId){const m=text.match(/External ticket ID:\s*([\s\S]*?)(?:\nPreferred language:|\nService:|\nEmail address:|\nResolution category:|\nContact by SMS:|\nDoc type:|\nContact phone:|\nOwner:|$)/i);externalTicketId=norm(m?.[1])||null;}const data={ticketId,externalTicketId};navigator.clipboard.writeText(JSON.stringify(data,null,2)).then(()=>showToast("JSON copié dans le presse-papiers")).catch(()=>showToast("Erreur copie clipboard"));})();
