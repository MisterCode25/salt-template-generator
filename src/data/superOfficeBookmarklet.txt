javascript:(()=>{const norm=s=>String(s||"").replace(/\s+/g," ").trim();const abs=u=>{try{return new URL(u,location.origin).href}catch{return u||null}};const msgDate=m=>m?.createdAt||m?.created||m?.date||m?.when||m?.messageDate||m?.messageDateTime||m?.sentAt||m?.receivedAt||m?.timestamp||null;const declaredImage=a=>String(a?.type||a?.contentType||a?.mimeType||"").toLowerCase().startsWith("image");const imageName=(url,alt,fallback)=>norm(alt)||decodeURIComponent(String(url||"").split("/").pop()?.split("?")[0]||"")||fallback;const showToast=msg=>{let t=document.createElement("div");t.textContent=msg;t.style.cssText="position:fixed;z-index:2147483647;left:50%;top:24px;transform:translateX(-50%);background:#111;color:#fff;padding:12px 18px;border-radius:10px;font:14px Arial,sans-serif;box-shadow:0 6px 22px rgba(0,0,0,.25);opacity:0;transition:opacity .2s ease;";document.body.appendChild(t);requestAnimationFrame(()=>t.style.opacity="1");setTimeout(()=>{t.style.opacity="0";setTimeout(()=>t.remove(),250)},1800)};const text=document.body.innerText;const infoValue=name=>{const wanted=String(name||"").replace(/:$/,"").toLowerCase();const labels=[...document.querySelectorAll(".HtmlInfoFields2_label,td,th,div,span")].filter(e=>norm(e.innerText).replace(/:$/,"").toLowerCase()===wanted);for(const label of labels){let tr=label.closest("tr");if(tr){const cells=[...tr.children];let i=cells.indexOf(label.closest("td,th"));for(let j=i+1;j<cells.length;j++){const v=norm(cells[j].innerText);if(v&&v!==norm(label.innerText))return v;}}let td=label.closest("td,th");let next=td?.nextElementSibling;while(next){const v=norm(next.innerText);if(v)return v;next=next.nextElementSibling;}}return""};const ticketId=text.match(/REQUEST\s+(\d+)/)?.[1]||text.match(/\b(\d{8})\s+-\s+Votre demande/)?.[1]||null;const createdAt=infoValue("Created");let externalTicketId=infoValue("External ticket ID");if(!externalTicketId){const m=text.match(/External ticket ID:\s*([\s\S]*?)(?:\nPreferred language:|\nService:|\nEmail address:|\nResolution category:|\nContact by SMS:|\nDoc type:|\nContact phone:|\nOwner:|$)/i);externalTicketId=norm(m?.[1])||null;}const allowed=/\.(jpe?g|png|webp|gif|bmp|avif|pdf)(?:$|\?)/i;const isPdf=n=>/\.pdf$/i.test(n||"");const isImage=n=>/\.(jpe?g|png|webp|gif|bmp|avif)$/i.test(n||"");const seen=new Set();const attachments=[];const add=a=>{if(!a)return;const name=a.name||a.filename||a.fileName||a.title||"";const url=a.url||a.href||a.src||a.downloadUrl||"";const asImage=declaredImage(a);if(!name||!url)return;if(/download all/i.test(name))return;if(!allowed.test(name)&&!allowed.test(url)&&!asImage)return;const finalUrl=abs(url);const key=finalUrl||url;if(seen.has(key))return;seen.add(key);attachments.push({name:name,url:finalUrl,type:asImage||isImage(name)||/\.(jpe?g|png|webp|gif|bmp|avif)(?:$|\?)/i.test(url)?"image":isPdf(name)||/\.pdf(?:$|\?)/i.test(url)?"pdf":"file",size:a.size||a.sizeText||null,messageId:a.messageId||null,date:a.date||a.messageDate||a.createdAt||null});};const htmlFields=m=>[m?.html,m?.bodyHtml,m?.messageHtml,m?.htmlBody,m?.body,m?.message,m?.content,m?.text].filter(v=>/<img\b/i.test(String(v||"")));const addHtmlImages=(html,msg)=>{try{const doc=new DOMParser().parseFromString(String(html||""),"text/html");[...doc.querySelectorAll("img[src]")].forEach((img,i)=>{const src=img.getAttribute("src");const alt=img.getAttribute("alt")||img.getAttribute("title")||"";add({name:imageName(src,alt,`inline-image-${msg?.id||"message"}-${i+1}.jpg`),src,type:"image",messageId:msg?.id||null,date:msgDate(msg)})})}catch(e){}};const addDomInlineImages=()=>{try{[...document.querySelectorAll(".HtmlMessages2 img[src],.HtmlMessages2_message img[src],.HtmlMessage img[src],[id*='HtmlMessages2'] img[src],article img[src]")].forEach((img,i)=>{const w=Number(img.naturalWidth||img.width||img.getAttribute("width")||0);const h=Number(img.naturalHeight||img.height||img.getAttribute("height")||0);if(w&&h&&Math.max(w,h)<48)return;const src=img.getAttribute("src");const alt=img.getAttribute("alt")||img.getAttribute("title")||"";add({name:imageName(src,alt,`inline-image-${i+1}.jpg`),src,type:"image"})})}catch(e){}};try{Object.values(window.HtmlMessages2_data||{}).forEach(block=>(block.messages||[]).forEach(msg=>{(msg.attachments||[]).forEach(a=>add({...a,messageId:msg.id,date:msgDate(msg)}));htmlFields(msg).forEach(html=>addHtmlImages(html,msg))}))}catch(e){}addDomInlineImages();[...document.querySelectorAll("a[href]")].forEach(a=>add({name:norm(a.textContent)||a.getAttribute("download")||a.href.split("/").pop(),url:a.getAttribute("href")}));const data={ticketId,createdAt,externalTicketId,attachments};navigator.clipboard.writeText(JSON.stringify(data,null,2)).then(()=>showToast("JSON copié dans le presse-papiers")).catch(()=>showToast("Erreur copie clipboard"));})();
